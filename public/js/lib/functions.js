function approach (goal, cur, delta){	var dif = goal - cur;	if (dif > delta)		return cur + delta;	if (dif < -delta)		return cur - delta;	return goal;}function getCart(fromObj, toObj){	// takes a from x+y and a to x+y and returns their cartesian offset	var result = {		x: (toObj.x - fromObj.x),		y: (toObj.y - fromObj.y)	};	return result;}function cart2pol(x, y){	// convert cartesian co-ords to polar co-ords	var magnitude = Math.sqrt(x*x + y*y);	var radians = Math.atan2(x, y);	var degrees = radians * (180 / Math.PI);	var result = { 		mag: magnitude, 		rad: radians, 		deg: degrees 	};	return result;}
function pol2cart(degrees, mag){	// Convert polar to cartesian	var x = mag * Math.sin(degrees);	var y = mag * Math.cos(degrees);	return {x: x, y: y};}